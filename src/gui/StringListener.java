package gui;

public interface StringListener {
	/*
	 * named using the convention of ActionListener wherein the method that must be
	 * implemented is actionPerformed().
	 */

	public void textEmitted(String text);

}
